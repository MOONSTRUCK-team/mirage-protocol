type Nat = nat;
type Principal = principal;

service : {
  get_or_create_nft_collection: (text) -> (variant { Ok: Principal; Err: text });
  get_nft_collection: (text) -> (opt Principal);
  get_all_collections: () -> (vec record { text; Principal; }) query;
}
